"""experiments/run_evaluation.py — beyond-accuracy evaluation.

STUB. Once pipeline.step03_evaluation is implemented, this script will:
  - load per-user metrics .npz produced by run_baselines / run_cars / run_proposed,
  - compute fairness (KL Divergence, Long-tail Rate) globally and per-situation,
  - compute interpretability artefacts (archetype characterisation, labels),
  - dump tables and figures to outputs/evaluation/.
"""

import sys


def main() -> int:
    print("experiments/run_evaluation.py — STUB.")
    print("  pipeline/step03_evaluation/ contains only stubs for now.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
